# services/inference.py

def run_inference(image):
  # Lógica para rodar a inferência no modelo de captcha
  # Exemplo: carrega o modelo e faz a predição com base na imagem
  result = "predicted_result"  # Isso é apenas um exemplo
  return result
